# blastengine SMTP認証情報（管理画面 > SMTP設定 で確認）
SMTP_HOST=（blastengineのSMTPホスト例: smtp.engn.jp）
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=（blastengineのSMTPユーザー名）
SMTP_PASS=（blastengineのSMTPパスワード）

# APIキー（kintoneカスタマイズ側の API_KEY 定数と同じ値を設定）
API_KEY=（ランダムな強力な文字列を設定してください）

# サーバーポート
PORT=3000
